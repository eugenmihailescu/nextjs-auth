export type LoginProvidersType = {
  google?: boolean;
  github?: boolean;
  spotify?: boolean;
};

export enum LoginProvidersTypes {
  google = 'google',
  github = 'github',
  spotify = 'spotify',
}
